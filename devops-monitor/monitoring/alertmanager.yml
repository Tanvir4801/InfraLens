global:
  resolve_timeout: 5m

route:
  receiver: dev-null
  group_by: ["alertname", "severity"]
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h

receivers:
  - name: dev-null
    # Add email, Slack, webhook, or PagerDuty configs here later.
    webhook_configs: []

inhibit_rules: []
